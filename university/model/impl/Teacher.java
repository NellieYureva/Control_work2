package model.impl;

import model.User;

public class Teacher extends User{

    public Teacher(int id, String name, String lastName,int groupId) {
        super(id, name, lastName, groupId);        
    }    
            
}
